Master programs:
  makefile
  makefile.base

Programs to generate the corpora
  makefile.corpus
  collections.sed
  comments.awk
  errors.awk
  fields.awk
  generate_corpus.py
  latex.sed
  posts.awk
  related.awk
  slts.awk
  slts.sed
  task2_corpus.awk
  [template_minimal_v3.html -- template file]
  titles.awk

Programs to generate the index
  makefile.index
  latex_filter_stopwords.py

Programs to query the corpora
  makefile.query
  extract_formulas_task2.py
  task2.awk
  tasks.awk
  unicode2asii.py

